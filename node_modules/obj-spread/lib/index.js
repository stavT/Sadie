"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ObjSpread = void 0;
var ObjSpread = /** @class */ (function () {
    function ObjSpread(fromClass, toClass, converter) {
        var funcsNames = Object.getOwnPropertyNames(fromClass);
        funcsNames.forEach(function (name) {
            var srcFunc = fromClass[name];
            if (typeof srcFunc == 'function') {
                Object.defineProperty(toClass.prototype, name, {
                    value: function () {
                        var args = __spreadArray([], arguments);
                        args.unshift(converter(this));
                        return srcFunc.apply(null, args);
                    }
                });
            }
        });
    }
    return ObjSpread;
}());
exports.ObjSpread = ObjSpread;
